/*var min = getmins('CrossedXclaims', 'assignedClaims',,, 1); // CrossedXclaims quota

for (var i = 0; i < 5; i++) // задаване на броя отговори, които трябва да се изберат
{
    if (min != 0 && min != null && min.length > 0) {
        f('assignedClaims')[min[i].code].set('1');
    }
}

********************************    
var min = getmins('CrossedXclaims', 'C2Claims','assignedClaims',, 1);
for (var i = 0; i < 2; i++) // задаване на броя отговори, които трябва да се изберат
{
    if (min != 0 && min != null && min.length > 0) {
        f('C2Claims')[min[i].code].set('1');
    }
}*/